import { ReactLearnWordsAPI } from '../../API/getWords';
import { IWordCard } from './consts';

// export const getWordsPage = (group: number, page: number) => {
//   const api = new ReactLearnWordsAPI();
//   const words = api.getWords(group, page);
//   words.then((card) => {
//     console.log(card);
//     return card;
//   });
// };
